@require "~styles/_utils";
@require "~styles/_vars";

a
  display: inline-block
  color: $color.white
  background: $color.purple
  border-radius: 4.5rem
  font-weight: bold
  padding: 1.4rem 4rem
  font-size: 1.6rem
  basic-transition(background)

  &:hover,
  &:active
    background: $color.pink
