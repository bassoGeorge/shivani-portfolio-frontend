@require "~styles/_vars"
:host
  display: block
  min-height: "calc(100% - %s)" % $navHeight
  padding: $cardGap
  background-color: $color.white

  &.off-white-page
    background-color: $color.off-white
